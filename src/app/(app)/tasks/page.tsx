export default function TasksPage() {
  return (
    <div className="space-y-2">
      <h1 className="font-display text-3xl font-semibold">My Tasks</h1>
      <p className="text-sm text-muted-foreground">Due today / overdue / upcoming — later.</p>
    </div>
  );
}
